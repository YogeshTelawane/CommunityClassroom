1. What is a Git?
It is a version control system used across the world.

2. Basic commands of git

a. git init - It is used to initialze your project with git.
b. git status - It shows files which are not tracked by git
c. git add - Adds the code to the staging area
d. git commit -m "comment" - It 
e. git log -  this show all the commits with data, time and comment